{
  "name": "adapos",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter auth dev",
    "build": "pnpm --filter auth build",
    "typecheck": "pnpm --filter auth typecheck && pnpm --filter '@adapos/auth' typecheck",
    "test": "pnpm --filter auth test && pnpm --filter '@adapos/auth' test",
    "deploy": "pnpm --filter auth deploy",
    "lint": "biome check ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "typescript": "^5.7.3",
    "vitest": "^4.1.0"
  }
}
